package com.example.library.repositories.avatarBooks;

import com.example.library.entities.avatarBooks.AvatarBooks;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AvatarBooksRepository extends JpaRepository<AvatarBooks, Long> {
}
